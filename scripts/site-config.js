window.RICHLAND_CONFIG = {
  // Set this to the ops backend when inquiry, PO, and portal flow move into the new backend.
  apiBaseUrl: "http://localhost:8787",
  // Legacy fallback for simple form delivery before the ops backend is fully connected.
  formspreeEndpoint: ""
};
